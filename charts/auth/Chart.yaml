apiVersion: v2
name: auth-service
description: A service managing user accounts data and providing authentication logic for the application

type: application
version: 1.0.1
appVersion: "1.0.0"

dependencies:
  - name: common-chart
    version: "1.0.1"
    repository: "file://../common/common-chart"

  - name: postgresql
    version: "18.1.1"
    repository: "oci://registry-1.docker.io/bitnamicharts"