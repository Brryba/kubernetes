apiVersion: v2
name: order-service
description: A service storing items and allowing users to make the orders

type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: common-chart
    version: "1.0.0"
    repository: "file://../../common/common-chart"